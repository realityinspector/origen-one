# Test environment variables
# This is the URL to the external PostgreSQL database for synchronization testing
# Format: postgresql://username:password@hostname:port/database

# Using a separate test database for synchronization
EXTERNAL_DB_URL=postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/origen_sync_test

# JWT Secret for authentication
JWT_SECRET=test-jwt-secret-for-auth-token-generation

# Session secret for cookies
SESSION_SECRET=test-session-secret-key

# Database URL (main application database)
# This should match your current DATABASE_URL environment variable
# The script will use this to connect to your main database
DATABASE_URL=${DATABASE_URL}