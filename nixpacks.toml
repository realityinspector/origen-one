[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm ci --omit=dev=false",
  "cd client && npm ci"
]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "npm run deploy"
